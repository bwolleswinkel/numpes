[project]
name = "numpes"
dynamic = ["version"]
dependencies = [
  "numpy",
  "scipy",
]
requires-python = ">=3.14"
authors = [
  {name = "Bart Wolleswinkel", email = "b.wolleswinkel@tudelft.nl"}
]
description = "Numerical operations on polytopes, ellipsoids, and subspaces"
readme = "README.md"
license = "GPL-3.0"
keywords = [
  "polytope",
  "ellipsoid",
  "subspace",
  "control",
  "scientific-computing",
]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Programming Language :: Python"
]

[project.optional-dependencies]
cdd = [
  "pycddlib",
]
extra = [
  "matplotlib",
  "cvxpy",
  "pulp",
]
full = [
  "pycddlib",
  "matplotlib",
  "cvxpy",
  "pulp",
]

[dependency-groups]
tests = [
  "numpes[full]",
  "tox",
  "pytest",
  "hypothesis",
]
dev = [
  "numpes[full]",
  "mypy",
  "scipy-stubs",
  "types-matplotlib",
  "isort",
  "pylint",
  "ruff",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"
raw-options = { local_scheme = "no-local-version" }

[tool.tox]
env_list = [
  "base",
  "cdd",
  "extra",
  "full",
]

[tool.tox.install_command]
install_command = "uv pip install {opts} {packages}"

[tool.tox.env.base]
deps = [
  "pytest",
  "pytest-cov",
  "hypothesis",
]
commands = [
  [
    "pytest",
    "--cov=numpes",
    "--cov-report=term-missing",
    "--cov-report=",
    "--no-cov-on-fail",
    "-rs",
    "tests",
  ]
]

[tool.tox.env.cdd]
extras = ["cdd"]
deps = [
  "pytest",
  "pytest-cov",
  "hypothesis",
]
commands = [
  [
    "pytest",
    "--cov=numpes",
    "--cov-report=term-missing",
    "--cov-report=",
    "--no-cov-on-fail",
    "-rs",
    "tests",
  ]
]

[tool.tox.env.extra]
extras = ["extra"]
deps = [
  "pytest",
  "pytest-cov",
  "hypothesis",
]
commands = [
  [
    "pytest",
    "--cov=numpes",
    "--cov-report=term-missing",
    "--cov-report=",
    "--no-cov-on-fail",
    "-rs",
    "tests",
  ]
]

[tool.tox.env.full]
extras = ["full"]
deps = [
  "pytest",
  "pytest-cov",
  "hypothesis",
]
commands = [
  [
    "pytest",
    "--cov=numpes",
    "--cov-report=term-missing",
    "--cov-report=",
    "--no-cov-on-fail",
    "-rs",
    "tests",
  ]
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "--import-mode=importlib --strict-markers"
testpaths = ["tests"]
markers = [
  # Function-based markers
  "enum_verts: tests for vertex enumeration functionality",
  "enum_facets: tests for facet enumeration functionality", 
  "display: tests for plotting and display functionality",
  # Module-based markers
  "utils: tests for the utils subpackage",
  # Test type-based markers
  "unit: unit tests",
  "coupled: coupled test with two or more functions that are being tested together",
]

[tool.coverage.run]
source = ["numpes"]
branch = true
data_file = "/tmp/.coverage"

[tool.coverage.report]
exclude_lines = [
  "pragma: no cover",
  "def __repr__",
  "raise AssertionError",
  "raise NotImplementedError",
]
skip_covered = false
skip_empty = true
show_missing = true

[tool.pylint.format]
max-line-length = 120

[tool.pylint.basic]
good-names = ["A", "A_eq", "Ab", "Ab_eq", "M", "Q", "R", "G"]

[tool.isort]
line_length = 9999

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "F403"]  # Ignore unused imports (F401) and wildcard imports (F403) only in __init__.py files
