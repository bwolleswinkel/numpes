"""The private config sub-package handles global configuration settings as well as defining some functionality which is used in the internal structuring of the code.

Modules
-------
global
    This module defines the global configuration settings for NumPES, including numerical tolerances, solver settings, and assignment/initialization behavior.
multipledispatch
    This module defines the multiple dispatch functionality used in NumPES, allowing for function overloading based on argument types in the Polytope constructor.
wraps
    This module defines a decorator for wrapping functions, used in for instance factory patterns.

"""